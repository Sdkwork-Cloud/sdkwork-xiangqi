# Packages

Shared TypeScript contracts consumed across the XIANGQI workspace.

| Package | Purpose |
| --- | --- |
| `packages/common/XIANGQI/sdkwork-xiangqi-contracts` | Cross-surface DTOs and helpers using `@sdkwork/utils` |

Architecture-local packages for PC UI live under `apps/sdkwork-xiangqi-pc/packages/`.
