schemaVersion: 1
kind: sdkwork.database.drift-policy
moduleId: XIANGQI
rules:
  - id: table-registry-match
    severity: error
    description: Live database tables must match contract table registry.
